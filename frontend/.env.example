# URL of the backend API, without a trailing slash. Defaults to http://localhost:3001 when unset.
# In production (Vercel), set this to the deployed Render backend URL.
VITE_API_URL=http://localhost:3001
